---
import TechStackOrbit from '@/components/effects/TechStackOrbit.astro';
import { parseText } from '@/utils/textParser';
const pt = (text: string) => parseText(text);
---

<section
  id="projects"
  class="relative container mx-auto px-4 pt-5 md:pt-10 pb-20 lg:px-8 xl:max-w-5xl"
>
  <div
    class="xs:-bottom-25 absolute right-0 -bottom-25 left-0 w-full sm:-bottom-30 md:-bottom-40 lg:-bottom-4"
  >
    <TechStackOrbit />
  </div>

  <div class="container mx-auto px-4 lg:px-8 xl:max-w-6xl">
    <div class="mb-12 space-y-2 text-center">
      <h2 class="font-serif text-3xl font-bold md:text-4xl" set:html={pt('Proyectos')} />
      <h3 class="text-2xl" set:html={pt('//Una selección de mis [[**Creaciones Digitales**]]//')} />
      <p
        class="text-light-2 mx-auto mt-4 mb-8 max-w-2xl text-center md:mb-14"
        set:html={pt(
          'Aquí hay algunos de los proyectos que tuve la suerte de haber trabajado combinando experiencia técnica con visión creativa para ofrecer soluciones únicas.'
        )}
      />
      <p
        class="text-light-2 mx-auto mb-14 max-w-2xl text-center text-xl"
        set:html={pt('Mis herramientas')}
      />
    </div>

    <div class="mt-12 space-y-16">
      <!-- Introduction -->
    </div>
  </div>
</section>
